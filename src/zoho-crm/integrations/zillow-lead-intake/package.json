{
  "name": "gh-zillow-lead-intake",
  "version": "1.1.0",
  "private": true,
  "description": "Secure Zillow Rentals lead intake webhook that normalizes Zillow form-urlencoded callbacks and upserts raw inquiries into Zoho CRM Leads.",
  "main": "src/index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "check": "node --check src/index.js",
    "test": "node --test test/*.test.js",
    "ci": "npm run check && npm test"
  }
}
