{
  "name": "gh-zillow-lead-intake",
  "version": "1.0.0",
  "private": true,
  "description": "Secure Zillow rental lead intake webhook that upserts applicants and rental applications into Zoho CRM.",
  "main": "src/index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "check": "node --check src/index.js",
    "test": "node --test test/*.test.js",
    "ci": "npm run check && npm test"
  }
}
